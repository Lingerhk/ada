#### Background
```shell
OS: Ubuntu 24.04 LTS
```

#### Dependencies
```shell
sudo apt-get install cmake make gcc g++ flex libfl-dev bison libpcap-dev libssl-dev python3 python3-dev swig zlib1g-dev libkrb5-dev libmaxminddb-dev libhiredis-dev
```

#### Build
```shell
cd zeek-7.1.0
./configure

```