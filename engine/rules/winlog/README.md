### Windows Events Logs规则


#### 规则File名定义
> 规则编号(ID)全部使用四位数字开头，然后再接连接符`-`，再接具体的规则名称，规则名称之间的单词使用下划线`_`连接，文件后缀统一使用`.yml`。形式为`0102-sigma_rule_name.yml`。

各子类型规则编号规范如下:
- `0000` 内部规则，该规则产生的数据主要用于缓存数据。
- `01xx` 信息探测类
- `02xx`
- `03xx`
- `04xx`
- `05xx`