# Kibana server configuration
server.host: "0.0.0.0"
server.port: 5601
server.basePath: "/kibana"
server.rewriteBasePath: true

# Elasticsearch connection
elasticsearch.hosts: ["http://elasticsearch:9200"]
elasticsearch.username: "kibana_system"
elasticsearch.password: "HszHApENsYWZXgq9mxOC"

# Encryption key for saved objects
xpack.encryptedSavedObjects.encryptionKey: "ada_kibana_encryption_key_min32chars_long"
